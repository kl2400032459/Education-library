{
  "name": "education-library-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix resource-library-ui",
    "build": "npm run build --prefix resource-library-ui",
    "preview": "npm run preview --prefix resource-library-ui",
    "lint": "npm run lint --prefix resource-library-ui"
  }
}
